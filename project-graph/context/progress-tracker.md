# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- 09 Share dialog — `Share` button in the editor navbar, collaborator list/invite/remove APIs, Clerk name/avatar enrichment (done)

## Current Goal

- Pick the next feature spec after 09 (likely the canvas, which replaces `CanvasPlaceholder` inside `/editor/[roomId]`).

## Completed

- 01 Design system (`context/feature-specs/01-design-system.md`)
  - shadcn/ui primitives added via CLI, unmodified: Card, Dialog, Tabs, Textarea, ScrollArea (Button, Input already present).
  - `lucide-react` installed.
  - `cn()` comes straight from the `cn` npm package; there is no `lib/utils.ts` wrapper.
  - `app/globals.css` now holds the `ui-context.md` dark tokens; shadcn semantic variables map onto them; light theme removed.
  - `<html>` carries the `dark` class so components' `dark:` variants apply.
  - Verified: `tsc`, `eslint` and `next build` pass with a page importing all seven primitives; `cn()` merge checks pass; `/sign-in` renders dark (body `#080809`, `color-scheme: dark`).

- 02 Editor chrome (`context/feature-specs/02-editor.md`)
  - [x] `components/editor/editor-navbar.tsx` — `h-14` grid bar with left/center/right sections; left holds the sidebar toggle (`PanelLeftOpen` / `PanelLeftClose`, `aria-expanded`); center and right empty; `bg-base` + `border-surface-border` bottom border. Props: `isSidebarOpen`, `onToggleSidebar`.
  - [x] `components/editor/project-sidebar.tsx` — `absolute` overlay (needs a `relative` workspace parent), slides in from the left via `translate-x`, `inert` + `aria-hidden` when closed; `isOpen` / `onClose` props; `Projects` header + close button; Tabs (My Projects, Shared) with empty placeholders; full-width `New Project` button with `Plus` icon (no handler yet).
  - [x] Dialog pattern — `components/editor/editor-dialog.tsx` (`EditorDialog`): wraps the stock shadcn Dialog with token styling (`rounded-3xl`, `bg-elevated`, backdrop blur); props `open`, `onOpenChange`, `title`, `description?`, `footer?`, `children`. No concrete dialogs built.
  - [x] Verified: `tsc` and `eslint` pass, `next build` passes; on a temporary page (since removed) the sidebar opened/closed without shifting content, tabs and empty states rendered, and the dialog showed title, description and footer actions.

- 03 Auth (`context/feature-specs/03-auth.md`)
  - [x] `@clerk/ui` installed. `lib/clerk-appearance.ts` exports `clerkAppearance`: Clerk's `dark` theme plus variables that point at `globals.css` tokens (`var(--accent-primary)`, `var(--bg-surface)`, ...). `ClerkProvider` in `app/layout.tsx` takes it, so the auth pages and `UserButton` share it. `colorBorder` is left unset on purpose (Clerk renders it at ~10% alpha, which hid `--border-default`), so borders come from `colorNeutral`.
  - [x] Custom passwordless flow removed (`components/auth/*`, `/sso-callback`, `/sign-in/continue`). `app/(auth)/sign-in/[[...sign-in]]` and `app/(auth)/sign-up/[[...sign-up]]` render stock `<SignIn>` / `<SignUp>`, and Clerk handles SSO callbacks under those catch-alls.
  - [x] `app/(auth)/layout.tsx` — two panels on `lg+` (wordmark, tagline and a text-only feature list on `bg-surface`; form centered on the right); form only below `lg`. No gradients or cards.
  - [x] `proxy.ts` — public routes are built from `NEXT_PUBLIC_CLERK_SIGN_IN_URL` / `NEXT_PUBLIC_CLERK_SIGN_UP_URL` (falling back to `/sign-in` / `/sign-up` when unset, with `(.*)` for nested steps) plus `/__clerk`. Everything else, including `/`, calls `auth.protect()`.
  - [x] `/` redirects to `/editor` when signed in and to `/sign-in` otherwise. The old landing page was removed.
  - [x] `/editor` — `app/editor/page.tsx` renders `components/editor/editor-workspace.tsx` (client component; owns sidebar state; mounts `EditorNavbar` + `ProjectSidebar`). `UserButton` sits in the navbar's right section.
  - [x] Env values changed, names unchanged: `NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up`, and both fallback redirects `=/editor`, in `.env.local` and `.env.example`.
  - [x] Follow-up: removed the dashboard (`app/(app)/*` incl. its layout, `components/app-shell/*`, `components/dashboard/*`); `/editor` is the only signed-in surface. App renamed to **Structured** (metadata title, auth wordmark, token comment, `package.json` name `structured`).
  - [x] Verified: `tsc`, `eslint` and `next build` pass. In a browser, signed-out `/editor` redirects to `/sign-in?redirect_url=...`. `/sign-in` shows two panels at 1440px. `/sign-up` shows the form only at 390px with no horizontal scroll. Colors come from the tokens. Not yet exercised: the signed-in path (`/` → `/editor`, `UserButton` menu), because no test account was available.

- 04 Project dialogue (`context/feature-specs/04-project-dialogue.md`)
  - [x] `components/editor/editor-home.tsx` — centered empty state on `/editor`: heading, description and a `New Project` button with a `Plus` icon. No cards, no navbar/sidebar changes.
  - [x] `hooks/use-project-dialogs.ts` — single owner of dialog kind (`create` | `rename` | `delete`), target project, shared name input, derived slug and `isSubmitting`. Closing leaves form values in place so they don't flicker during the exit animation; each open resets what it needs. `submit()` is async and holds `isSubmitting` past the close (the next open clears it), plus a ref guard against double-clicks landing in one frame — persistence lands with the project API slice at the marked `TODO`.
  - [x] `lib/projects/slug.ts` — `toProjectSlug()`: lowercase, expand the non-decomposable Latin letters (`ß ø æ œ ł đ ð þ`), strip diacritics (NFD), non-alphanumerics to hyphens, trim hyphens. A name that transliterates to nothing falls back to a deterministic `project-<djb2 base36>` so the create dialog can still accept it. Checked against "Payments API v2" → `payments-api-v2`, "Ünïcode Test" → `unicode-test`, "Straße" → `strasse`, "決済基盤" → `project-<hash>`, "###" → `project-<hash>`, "" → `""`.
  - [x] `lib/projects/mock.ts` + `types/project.ts` — `MY_PROJECTS` / `SHARED_PROJECTS` stand-ins. Ownership is not a field on `Project`: it comes from the list a project is in, so the sidebar passes rename/delete handlers only for owned projects.
  - [x] `components/editor/project-dialogs.tsx` — all three dialogs in one client component driven by the hook's state; the editor workspace mounts it once as `<ProjectDialogs dialogs={dialogs} />`.
    - Create: name input plus a live `Slug <value>` line in mono that updates on every keystroke; confirm disabled while the slug is empty.
    - Rename: prefilled input, current name in the description, `initialFocus` on the input, and a `<form>` so Enter submits (the footer button uses `form="rename-project-form"`).
    - Delete: confirmation copy only, no input, `variant="destructive"` confirm.
  - [x] `project-sidebar.tsx` — project lists with per-item rename/delete icon buttons (owned tab only; the Shared tab renders names with no actions), a mobile-only backdrop scrim (`md:hidden`) that closes the sidebar on an outside tap, and `New Project` wired to the create dialog.
  - [x] `editor-dialog.tsx` — added an optional `initialFocus` prop forwarded to the Base UI popup.
  - [x] Verified: `tsc`, `eslint` and `next build` pass. Driven in a browser through a temporary public `/preview-check` route (since removed, proxy restored): editor home renders, both `New Project` entry points open the create dialog, the slug preview tracks typing, rename prefills/auto-focuses and submits on Enter, delete shows the destructive confirm, shared projects show no actions, and at 390px tapping the scrim closes the sidebar. Console clean apart from Clerk's pre-existing dev-keys warning.

- 05 Prisma (`context/feature-specs/05-prisma.md`)
  - [x] `prisma/models/project.prisma` (multi-file schema; `prisma/schema.prisma` keeps the generator and datasource). `Project`: `ownerId` (Clerk user ID), `name`, optional `description`, `status` (`ProjectStatus` enum `DRAFT` | `ARCHIVED`, default `DRAFT`), optional `canvasJsonPath`, `createdAt`/`updatedAt`; indexes on `ownerId` and `createdAt`. `ProjectCollaborator`: `project` relation with `onDelete: Cascade`, `email`, `createdAt`; unique `[projectId, email]`; indexes on `email` and `[projectId, createdAt]`. The only fields beyond the spec are the ones Prisma requires: the `cuid()` `id` on each model and the `Project.collaborators` back-relation.
  - [x] `lib/prisma.ts` exports one `prisma` instance. A `DATABASE_URL` starting with `prisma+postgres://` goes to `new PrismaClient({ accelerateUrl })` (built into Prisma 7, so no `@prisma/extension-accelerate`); anything else goes to `@prisma/adapter-pg`. Outside production the instance is cached on `globalThis`, so hot reloads reuse it. Throws if `DATABASE_URL` is unset.
  - [x] Client generated to `app/generated/prisma` (gitignored), imported as `@/app/generated/prisma/client`.
  - [x] `prisma7.config.ts` loads `.env.local` before `.env`, matching Next.js precedence. Before this change the CLI read only `.env`, which holds a local `prisma dev` URL, while the app reads the remote Prisma Postgres URL in `.env.local`, so they would have hit different databases. Chidubem chose the remote database.
  - [x] Migration `20260918035438_init` applied to the remote Prisma Postgres (the database was empty beforehand).
  - [x] Verified: `prisma validate`, `tsc`, `eslint` and `npm run build` pass. A throwaway script run against the migrated database showed that repeated imports return the same instance and it is cached on `globalThis`, `status` defaults to `DRAFT`, a duplicate project/email fails with `P2002`, and deleting a project cascades to its collaborators. The test row was removed. The Accelerate branch constructs a client, but it has not been queried because the local `prisma dev` server was not running.

- 06 Project APIs (`context/feature-specs/06-project-apis.md`)
  - [x] `app/api/projects/route.ts` — `GET` returns `{ projects }` for `ownerId = userId`, newest first (`createdAt desc`). `POST` returns `201 { project }`; `ownerId` is the Clerk user ID; no `id` is passed, so the schema's `cuid()` default assigns it. A missing, empty or whitespace-only `name` becomes `Untitled Project`; other names are trimmed.
  - [x] `app/api/projects/[projectId]/route.ts` — `PATCH` takes `{ name }` (required, trimmed, non-empty) and returns `{ project }`; `DELETE` returns `204` with no body (collaborators cascade). Both run `denyUnlessOwner` first (404 if the project doesn't exist, 403 if someone else owns it). In `PATCH` it runs before the body is parsed, so a non-owner gets 403 even when the body is invalid (a CodeRabbit finding: originally a bad body returned 400 first). The write itself also filters on `ownerId`, so a project deleted between the check and the write returns 404 (Prisma `P2025`) rather than 500.
  - [x] Shared helpers in `lib/` (moved into `lib/http/` and `lib/projects/` after spec 06, see Architecture Decisions): `http/api-response.ts` (`errorResponse`, error body is always `{ error }`), `http/request-body.ts` (`readJsonObject`: an empty body reads as `{}`, a non-object or invalid JSON returns 400), `projects/name.ts` (`DEFAULT_PROJECT_NAME`, create/rename parsing), `projects/ownership.ts` (`denyUnlessOwner`). `isRecordNotFound` started in the ownership file and now lives in `lib/prisma.ts`, since it classifies any Prisma P2025 error, not a project one.
  - [x] `proxy.ts` — signed-out requests to `/api(.*)` get `401 { error: "Unauthorized" }` from the proxy. Before this, `auth.protect()` answered them with 404. Pages still use `auth.protect()`. Each handler also checks `auth()` itself, because it needs `userId`.
  - [x] Verified: `npm run build` (both routes listed as dynamic), `tsc` and `eslint` pass. Against `next start`, signed-out `GET`/`POST`/`PATCH`/`DELETE` all return `401` JSON from the proxy. The handlers were then called against the migrated database with Clerk's `auth()` stubbed to two throwaway user IDs, and all 23 checks passed: 401 when signed out; create with no body, a blank name and a trimmed name; `cuid` IDs; 400 for a non-string name, invalid JSON, an array body and an empty or missing rename; each user lists only their own projects; a non-owner's `PATCH`/`DELETE` returns 403 and leaves the row unchanged; the owner can rename (200) and delete (204); a missing project returns 404. The test rows were removed afterwards. Not exercised: a real Clerk session end to end (still no test account).

- 07 Wire editor home (`context/feature-specs/07-wire-editor-home.md`)
  - [x] `lib/projects/queries.ts`: `getProjectLists(user)` returns `{ myProjects, sharedProjects }` as `{ id, name }`, newest first. Owned projects match `ownerId`. Shared projects match `ProjectCollaborator.email` against the user's **verified** Clerk email addresses and exclude projects the user owns. The spec referred to an "existing project data helper", but none existed, so this was added. `GET /api/projects` is unchanged, so its response shape stays the same.
  - [x] `app/editor/page.tsx` is an async server component: `currentUser()` → `getProjectLists` → `EditorWorkspace`. Nothing is fetched client-side on first load. `lib/projects/mock.ts` was deleted, and `Project` lost its `slug` field (it is now `{ id, name }`, and `id` is the room ID).
  - [x] `app/editor/[projectId]/page.tsx` — minimal workspace route (none existed for "navigate to the new workspace" to target). It has the same chrome, shows the project name in the navbar center and leaves the canvas area empty until the canvas spec. It returns 404 unless the project is in the user's owned or shared list.
  - [x] `lib/projects/id.ts` — `createProjectIdSuffix()` (6 random `[a-z0-9]` characters from `crypto.getRandomValues`), `toProjectId(name, suffix)` (`<slug>-<suffix>`, with the slug cut so the ID is at most 64 characters) and `parseNewProjectId()` for the API.
  - [x] `POST /api/projects` accepts an optional `id`: lowercase alphanumerics and single hyphens, at most 64 characters, otherwise 400. A taken ID returns 409 (`isUniqueConstraintViolation`, P2002, in `lib/prisma.ts`). With no `id`, the `cuid()` default still applies.
  - [x] `lib/projects/requests.ts` — browser-side `createProject` / `renameProject` / `deleteProject`. Each throws an `Error` carrying the API's `{ error }` message.
  - [x] `hooks/use-project-actions.ts` (`useProjectActions`) replaces `use-project-dialogs.ts`. Each create dialog draws one suffix, so the previewed room ID is exactly the one submitted. Create: `POST` → `router.push('/editor/<id>')`. Rename: `PATCH` → `router.refresh()`. Delete: `DELETE`, then `router.replace('/editor')` if it was the active workspace (read from `useParams`), otherwise `router.refresh()`. On failure the dialog stays open, shows the API message (`role="alert"`) and re-enables its confirm button. While a request is in flight the dialog is locked (2026-09-19, review finding): `closeDialog` and the three `open*` handlers do nothing, and Cancel is disabled. Escape and the backdrop go through `closeDialog`, so they are ignored too. Before this, a slow request could be dismissed and another dialog opened; the old request's completion then closed the new dialog and navigated or refreshed, and the new dialog's confirm looked enabled but was silently rejected by the `inFlight` guard.
  - [x] Dialogs: create shows `Room ID <slug>-<suffix>` live; rename pre-fills the current name; delete names the project in its description.
  - [x] Verified: `tsc`, `eslint` and `npm run build` pass (`/editor` and `/editor/[projectId]` are dynamic). 29 checks against the real database with Clerk `auth()` stubbed all passed: suffix format and uniqueness; room IDs for ASCII, CJK, 200-character and `Straße` names all pass server validation; 9 invalid IDs are rejected; `POST` with an ID returns 201 with the same ID; a duplicate returns 409; an invalid ID returns 400; no ID gives a cuid; owned lists are newest first with `{ id, name }` only; shared lists include only collaborator projects matched on a verified email, and never the user's own. Test rows were removed. In a browser, through a temporary public preview route with the API mocked in Playwright (since removed, `proxy.ts` restored): the room ID preview tracks typing with a stable suffix, the `POST` body `id` equals the preview, and create navigates to `/editor/<id>`. A 409 shows the error, keeps the dialog open and re-enables the button. Rename pre-fills, submits `PATCH` on Enter and refreshes. Deleting another project refreshes the current workspace; deleting the active one replaces to `/editor`, so Back skips the deleted workspace. Not exercised: a real signed-in session end to end (still no Clerk test account).

- 08 Editor workspace shell (`context/feature-specs/08-editor-workspace-shell.md.md`)
  - [x] Route renamed `app/editor/[projectId]` → `app/editor/[roomId]` (`git mv`). Next.js rejects two differently named dynamic segments side by side, and project ID = room ID, so this is a rename, not a second route. `useProjectActions` now reads `useParams().roomId`.
  - [x] `lib/project-access.ts` (the path the spec names; see Architecture Decisions):
    - `getCurrentIdentity()` returns `{ userId, email }` or null. `email` is the Clerk primary email, and it is null unless that address is verified. `currentUser()` is not cached (each call is a Clerk Backend API request), so a page calls this once and passes the identity on.
    - `findAccessibleProject(projectId, identity)` runs one `findFirst`: the project ID AND (`ownerId = userId` OR a collaborator row with `email = identity.email`). It returns `{ id, name }` or null. Missing and forbidden look the same.
  - [x] `getProjectLists` now takes a `ProjectIdentity` instead of a Clerk `User`, and matches Shared on the verified primary email, the same email the access check accepts. Before this, Shared matched *any* verified email, so a project shared to a secondary address would have been listed and then shown `AccessDenied` when opened. Nothing adds collaborators yet, so no existing data is affected. `/editor` uses `getCurrentIdentity` too.
  - [x] `app/editor/[roomId]/page.tsx` — async server component. Signed out → `redirectToSignIn()` (the configured `/sign-in`, with a `redirect_url` back to the room; `proxy.ts` redirects first anyway). The access check and the sidebar lists load in parallel. No access or a missing project → `<AccessDenied />`, replacing spec 07's `notFound()`.
  - [x] `components/editor/access-denied.tsx` — full-viewport centered layout on `bg-base`, `LockIcon`, heading plus one line of copy that doesn't reveal whether the project exists, and a `Back to projects` link to `/editor` styled with `buttonVariants`. It is a server component.
  - [x] `editor-navbar.tsx` — the `projectName` prop was replaced by an optional `room` prop (`name`, `isAiSidebarOpen`, `onToggleAiSidebar`). With a room open, the center shows the name as the page `h1` and the right section shows a disabled outline `Share` button (sharing is a later spec; disabled rather than a silent no-op) and a Sparkles AI toggle with `aria-expanded` and `aria-controls="ai-sidebar"`. The editor home passes no `room` and is unchanged.
  - [x] `project-sidebar.tsx` — new `activeProjectId` prop. The matching row in either tab gets `aria-current="page"`, `bg-accent-dim` and a `text-brand` name.
  - [x] `project-sidebar.tsx` — project names are now `next/link`s to `/editor/[id]` in both tabs (CodeRabbit review), so owned and shared projects can be opened from the sidebar. A link rather than an `onOpenProject` callback: opening is plain navigation, and a link gives prefetch, new-tab and keyboard focus for free. The workspace lives in the page, not a layout, so switching rooms should remount it and close the sidebar. Verified: `tsc` and `eslint` pass; not yet clicked through in a browser.
  - [x] `components/editor/canvas-placeholder.tsx` — fills the workspace area (`h-full` inside the `relative flex-1` region) on `bg-base` with a token-colored dot grid and a centered "Canvas coming soon" message naming the project.
  - [x] `components/editor/ai-sidebar.tsx` — right-hand floating overlay mirroring the project sidebar (`absolute`, `translate-x`, `inert` when closed, `w-80` capped to the viewport), with an `AI Assistant` header, close button and a coming-soon message. Closed by default. Only mounted with a room open.
  - [x] Verified: `tsc`, `eslint` and `npm run build` pass (`/editor/[roomId]` dynamic). 13 checks against the real database all passed: the owner gets `{ id, name }` (with or without an email); a collaborator gets access by primary email; a stranger, a collaborator with no verified email, a null email and a case-mismatched email are denied; a missing project returns null; the owner/collaborator/no-email/stranger sidebar lists agree with the access check. Test rows were removed. Against `next dev`, a signed-out `GET /editor/some-room` returns 307 to `/sign-in?redirect_url=…/editor/some-room`. In a browser, through a temporary public preview route with stand-in data (since removed, `proxy.ts` restored): at 1440px the canvas fills everything below the 56px navbar with no horizontal scroll; the navbar shows the name, the disabled Share button and the AI toggle; both sidebars open over the canvas; the current room is highlighted in My Projects and in Shared; the AI toggle reports `aria-expanded` and turns AI purple when open (the first version stayed white because ghost's `aria-expanded:text-foreground` beat a plain `text-ai-text`, so it now uses `aria-expanded:text-ai-text`); the layout holds at 390px; `AccessDenied` renders centered and links to `/editor`. Console: only Clerk's dev-keys warning. Not exercised: a real signed-in session end to end (still no Clerk test account).

- 09 Share dialog (`context/feature-specs/09-share-dialogue.md`)
  - [x] `app/api/projects/[projectId]/collaborators/route.ts`:
    - `GET` returns `{ collaborators }`, oldest invite first. It uses the same check as opening the room (`getCurrentIdentity` + `findAccessibleProject`), so the owner and collaborators can both read the list. A stranger or a missing project gets 404.
    - `POST { email }` returns `201 { collaborator }` and is owner only: `denyUnlessOwner` runs before the body is read (404/403). The email is trimmed, lowercased and loosely validated (`lib/projects/email.ts`: at most 254 characters, one `@`, a dot in the domain), otherwise 400. The owner's own primary email returns 400 "You already own this project". A repeat invite in any letter case returns 409 (the unique `[projectId, email]` index, P2002). A project deleted between the owner check and the insert returns 404 (P2003, new `isForeignKeyViolation` in `lib/prisma.ts`).
  - [x] `app/api/projects/[projectId]/collaborators/[collaboratorId]/route.ts` — `DELETE` returns 204 and is owner only (`denyUnlessOwner`). The delete is also scoped to `{ id, projectId, project.ownerId }`, so a collaborator ID from another project, or one already removed, returns 404 and touches nothing. Rows are addressed by collaborator ID, so emails never appear in URLs.
  - [x] `lib/projects/collaborators.ts` (server only) — `getCollaborators` / `addCollaborator` / `removeCollaborator` plus Clerk enrichment. It makes one `clerkClient().users.getUserList({ emailAddress, limit })` call per 100 emails (Clerk's filter cap; `limit` is set explicitly because the default is 10). `name` is the user's `fullName`, then their `username`, else null. `imageUrl` is Clerk's `imageUrl`. Only **verified** addresses are matched, so no one can borrow another person's name and avatar by adding that person's address unverified. If no user matches, or the Clerk call fails (logged), the response still succeeds and the row shows the email alone. There is no local user table.
  - [x] `types/collaborator.ts` — `Collaborator { id, email, name | null, imageUrl | null }`. `lib/projects/requests.ts` gained `fetchCollaborators` / `inviteCollaborator` / `removeCollaborator`.
  - [x] Email normalization (resolves the earlier open question): invites store emails trimmed and lowercased, and `getCurrentIdentity()` now lowercases the verified primary email too. A collaborator whose Clerk address has different letter case can now open the room, see it under Shared and read the list. Spec 08's check that "a case-mismatched email is denied" no longer holds, deliberately. No collaborator rows existed before this spec, so no data migration was needed.
  - [x] `hooks/use-share-dialog.ts` (`useShareDialog(projectId | null)`) holds the open state, the collaborator list, the invite form, the in-flight removals and the copy feedback.
    - Every open reloads the list, since other people may have changed it, and keeps the previous list on screen while it loads.
    - A version counter drops a load response that arrives after an invite or removal, so a stale list can't undo the change.
    - Invite is blocked until the first load finishes; otherwise it would discard that load.
    - Copy link writes `<origin>/editor/<roomId>` to the clipboard and shows `Copied!` for 2 seconds. Repeated clicks restart the timer, and it is cleared on unmount. A clipboard failure shows an error instead.
  - [x] `components/editor/share-dialog.tsx` — built on `EditorDialog`.
    - Owner view: an email input and `Invite` button (Enter submits; the button is disabled while empty, while an invite is in flight, or before the list loads). A failed invite shows an error below it (`role="alert"`, `aria-invalid`), and the dialog stays open.
    - List: each row has an avatar (`AvatarImage` when Clerk returned one, falling back to initials) and the name with the email beneath it, or the email alone. Owners get a per-row remove button labelled `Remove <name or email>`, which shows a spinner and is disabled while its request is in flight. The list shows a loading row until it arrives and "No collaborators yet." when empty.
    - Footer: `Copy link` / `Copied!` (with an `aria-live` announcement) and `Done`.
    - Collaborator view: read-only copy ("Only the owner can manage access."), the list with no remove buttons, and `Done` only.
  - [x] `components/ui/avatar.tsx` added via `shadcn add avatar`, unmodified (it already imports `cn` from `"cn"`).
  - [x] Navbar `Share` is enabled for anyone in a room (`room.onOpenShare`, `aria-haspopup="dialog"`). The workspace treats the active project as owned when it appears in `myProjects`, following the sidebar's "ownership is a list" rule. It passes `isOwner` to the dialog; the API enforces ownership independently.
  - [x] Verified: `tsc`, `eslint` and `npm run build` pass (both new routes are dynamic). The handlers were run against the real database with `@clerk/nextjs/server` stubbed (`auth`, `currentUser`, and `clerkClient().users.getUserList` with a fake directory), and all 31 checks passed:
    - 401 when signed out.
    - Owner: 400 for a non-object body, a missing email, an invalid email and their own email. An invite with surrounding spaces and mixed case is stored lowercased, and returns 201 with the Clerk name and avatar. A repeat invite in different case returns 409. An address only held unverified in Clerk shows the email alone. The list is oldest first.
    - Collaborator (mixed-case Clerk email): can open the room and `GET` the list, but `POST`/`DELETE` return 403. With an unverified email, `GET` returns 404.
    - Stranger: `GET` 404, `POST`/`DELETE` 403. A missing project returns 404 for `GET` and `POST`.
    - `DELETE`: another project's collaborator ID sent through the owner's own project returns 404 and the row survives. The owner's delete returns 204, and a second delete returns 404.
    - A Clerk lookup failure returns 200 with emails only. An insert on a missing project raises P2003.

    Test rows were removed. In a browser, through a temporary public preview route with the collaborator API mocked in Playwright (since removed, `proxy.ts` restored):
    - Owner at 1440px: `Share` opens the dialog with a loading row and a disabled `Invite`. A broken avatar URL falls back to initials. Enter invites `  New.Person@Example.com ` and clears the input. A duplicate shows "Already a collaborator" with `aria-invalid`. Remove disables its button mid-request, then drops the row. `Copy link` puts `http://localhost:3000/editor/payments-api-abc123` on the clipboard, shows `Copied!`, announces "Link copied" and reverts within 2 seconds.
    - Collaborator at 390px: read-only description, no email input, no remove or copy buttons, only `Done`, and no horizontal scroll.

    Console: only Clerk's dev-keys warning, plus the intentional 404 avatar and 409 responses. Not exercised: a real signed-in session end to end or a real Clerk directory lookup (still no Clerk test account).

## In Progress

- None.

## Next Up

- Add the next planned feature unit here.

## Open Questions

- The Clerk instance's application name is still "Project Graph" (the sign-in card reads "Sign in to Project Graph"). Rename it to Structured in the Clerk dashboard.
- `docs/adr/0001-v1-technology-foundations.md` still lists email/password auth. Whether a password field appears is now controlled by the Clerk dashboard settings.
- The generated Prisma client (`app/generated/prisma`) is gitignored, so a fresh clone or a Vercel build only works if something runs `prisma generate` first. Common fixes are a `postinstall` script or `prisma generate && next build`. Not added, because spec 05 doesn't cover it.
- `pg` warns that `sslmode=require` in the remote `DATABASE_URL` is treated as `verify-full` today and will change meaning in pg v9. Setting `sslmode=verify-full` explicitly keeps the current behaviour.
- `GET /api/projects` returns owned projects only. Collaborator (shared) projects are matched by email, which the API would need to read from Clerk. That belongs to whichever spec wires up the Shared tab.
- Project names have no maximum length: spec 06 doesn't set one and the column is unbounded `text`. Add a limit if a later spec asks for one.
- `AccessDenied` renders with HTTP 200. Next's `forbidden()` would send 403 but needs the experimental `authInterrupts` flag, and the spec asks for the component. Revisit if a real status code matters.
- Collaborator access and the Shared tab use the verified **primary** email only (spec 08's identity is "userId + primary email"). A collaborator invited at a secondary address won't see or open the project until that address is primary. Whichever spec adds collaborators should decide whether that is right.
- A `next start -p 3107` process (from an earlier verification session) was still running on 2026-09-19 and was left alone. It serves whatever `.next` build it started with; stop it if it isn't needed.
- Inviting stores a collaborator row only. No email or Clerk invitation is sent (spec 09 doesn't ask for one), so invitees learn about a project from the owner, e.g. through the copied link. An invitee without a Clerk account must sign up with that address and verify it before they can open the project.
- Spec 09 gives `Copy link` to owners only, so collaborators don't see it (they can still copy the address bar). The share dialog lists collaborators but not the owner, since the spec only mentions collaborators. Showing the owner at the top would help collaborators; add it if a later spec wants it.
- The share dialog enriches only on open, one Clerk Backend API request per 100 collaborators. Clerk's Backend API is rate limited; if lists grow large or dialogs are opened often, cache the profiles.
- The 64-character project/room ID cap is this codebase's choice (spec 07 names none). Liveblocks isn't installed yet; confirm its room ID limits when it lands.
- A 409 on create now draws a new room ID suffix in the same dialog (2026-09-18), so a retry after an ID clash goes through without reopening. `lib/projects/requests.ts` throws `ApiError` with the HTTP status so the hook can tell 409 apart. Remaining edge: if a create succeeds but its response is lost, the retry gets 409 against the user's own new project, and the next attempt creates a second project under the new suffix. Telling that apart would need a lookup of the conflicting ID's owner.
- `.env` still holds the local `prisma dev` URL. Both the CLI and the app now take `DATABASE_URL` from `.env.local`, so that value is only used if `.env.local` drops its `DATABASE_URL`.

## Architecture Decisions

- Collaborator emails are stored normalized (trimmed and lowercased; `lib/projects/email.ts`), and `getCurrentIdentity()` normalizes the signed-in email the same way (2026-09-19, spec 09). Every comparison between a stored collaborator and a Clerk address goes through `normalizeEmail`. Collaborator names and avatars are never stored: they are read from Clerk per request, and only for verified addresses (`lib/projects/collaborators.ts`). Reading the collaborator list uses the room access check (owner or collaborator); inviting and removing use `denyUnlessOwner`, plus a write scoped to the owner where Prisma allows it.

- Project access lives in `lib/project-access.ts` (2026-09-19, spec 08), at the top level because the spec names that path, although `code-standards.md` would otherwise place it at `lib/projects/access.ts`. It is the one definition of "who may open a project": `getCurrentIdentity()` for who is asking, `findAccessibleProject()` for owner-or-collaborator. The sidebar's Shared list uses the same identity, so the two can't disagree. Liveblocks token issuance should reuse `findAccessibleProject` when it lands. Owner-only mutations still go through `lib/projects/ownership.ts`.

- API routes answer signed-out requests with `401` JSON from `proxy.ts`, not `auth.protect()` (which returns 404 for non-page requests). The proxy still denies by default, so a new API route is guarded as soon as it exists. Handlers still call `auth()` for the `userId`. API error bodies are always `{ error: string }`. Owner-only mutations call `denyUnlessOwner` and also scope the write to `{ id, ownerId }`.
- `lib/` is grouped by domain (2026-09-18): `lib/http/` holds request/response helpers (`api-response.ts`, `request-body.ts`), `lib/projects/` holds project logic (`name.ts`, `slug.ts`, `ownership.ts`, `mock.ts`), and single-purpose infrastructure (`prisma.ts`, `clerk-appearance.ts`) stays at the top level. Prisma-specific helpers such as `isRecordNotFound` sit in `lib/prisma.ts` beside the client, not in the domain folder that first needed them. Helpers stay in `lib/` rather than `app/api/` because `proxy.ts` and client hooks import them too. There are no `index.ts` barrels: `lib/projects/` mixes client-safe files (`slug`, `name`) with a server-only one (`ownership` imports Prisma), and a barrel would let a client import pull Prisma into the browser bundle.
- Prisma CLI and the Next.js app must read the same `DATABASE_URL`: `prisma7.config.ts` loads `.env.local` then `.env`, and dotenv keeps the first value, the same precedence Next.js uses. Schema files live under `prisma/` (the config points at the folder), with models in `prisma/models/*.prisma`.

- Project ID = Liveblocks room ID (2026-09-18, spec 07). The client builds it as `<slug>-<6-char suffix>` so the room ID is readable, and sends it on `POST`. The server validates the format and relies on the primary key for uniqueness (409 on collision). There is no separate room ID column.
- Project dialog state and mutations live in one hook (`hooks/use-project-actions.ts`, formerly `use-project-dialogs.ts`) rather than in the sidebar, so the editor home and the sidebar open the same dialogs. The dialogs themselves are presentational and take values plus callbacks.
- Project ownership is a list, not a flag: the sidebar renders rename/delete only for the list it is given handlers for. When the API slice lands, "owned" becomes whatever the server returns for the signed-in user.
- The project sidebar hides its closed state with `inert` alone. `aria-hidden` was removed because Chrome warns when a focused descendant is hidden that way — which now happens whenever the mobile scrim closes the sidebar while a project action button has focus.
- `cn()` is the `cn` npm package (replaces `clsx` + `tailwind-merge`, both removed). Every file imports it directly as `import { cn } from "cn"` — the `lib/utils.ts` re-export was deleted on 2026-09-17 so there is one spelling. `shadcn add` only rewrites `@/`-prefixed specifiers, so a newly added component may still arrive importing `@/lib/utils`; rewrite that line to `"cn"`. If a wrapped engine is ever needed (`createEngine`/`CnConfig`), reintroduce `lib/utils.ts` and switch every import back to it rather than having two spellings.
- Dark only: design tokens live on `:root` in `globals.css`; there is no `.dark` override block. Token utilities: `bg-base|surface|elevated|subtle`, `text-copy-primary|secondary|muted|faint`, `border-surface-border`, `border-surface-border-subtle`, `text-brand`/`bg-brand`, `bg-accent-dim`, `ai`, `ai-text`, `error`, `success`, `warning`.
- Auth UI is Clerk's prebuilt components on separate `/sign-in` and `/sign-up` pages (replacing the earlier single custom passwordless page). Clerk styling goes only through `clerkAppearance` variables; Clerk internals are not restyled.

## Session Notes

- Spec 04's loading state was initially dead: `submit()` set `isSubmitting` and then called `closeDialog()`, which cleared it in the same React batch, so no render ever observed it and every `disabled={... || isSubmitting}` was decorative. `submit()` is now async, `closeDialog()` no longer clears the flag, and the open handlers do — so the disabled wiring is real before persistence arrives rather than needing a rewrite after.
- The create dialog gates on `name.trim()`, not on `slug`. Gating on the slug meant any name with no ASCII alphanumerics (CJK, Cyrillic, punctuation-only) produced an empty slug and a permanently inert confirm button with no error text, `aria-invalid` or `aria-describedby` to explain it — a dead end for every user. Rename already gated on the name, so the two dialogs now agree.
- Per spec, only the rename dialog submits on Enter; the create dialog submits from its footer button. Worth revisiting for consistency once both hit the API.
- `/editor` is behind Clerk and there is still no test account, so browser checks run through a temporary public route (`app/preview-check` + one public matcher in `proxy.ts`) that is deleted afterwards.
- Spec 03 conflicted with the existing custom auth flow, the single-page sign-in decision and the missing `/editor` route. Resolved with Chidubem: use Clerk components, add a separate `/sign-up`, and create a minimal `/editor`.
- Spec 02 does not define an editor route, so the chrome components are not mounted anywhere yet. (Superseded by 03: `/editor` now mounts them.)
- Spec 01 referred to an "existing dark theme in `globals.css`", but the file still had shadcn's stock light theme; the dark tokens were taken from `ui-context.md`. Existing auth/dashboard pages (both since replaced/removed) switched to dark as a result.
- Installing Dialog prompts to overwrite `button.tsx`; answer no. `components/ui/input-otp.tsx` carries custom OTP cell styling from the auth PR, so never re-add it with `--overwrite`.
