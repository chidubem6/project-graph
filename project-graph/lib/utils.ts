// Tailwind class merging (clsx-style conditionals + conflict resolution).
// Backed by shadcn's `cn` engine, which newer shadcn/ui components import directly.
export { cn, type ClassValue } from "cn"
